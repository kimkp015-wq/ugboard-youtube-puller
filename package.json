{
  "name": "ugboard-puller",
  "version": "1.0.0",
  "scripts": {
    "fetch:youtube": "python3 src/fetch_youtube_ids.py",
    "build": "npm run fetch:youtube && wrangler build",
    "deploy": "npm run fetch:youtube && wrangler deploy",
    "dev": "wrangler dev --local"
  },
  "dependencies": {
    "wrangler": "^3.0.0"
  }
}
